<!-- ---
// Import necessary components and classes
import { Markdown } from '../utils/Markdown';

// Import necessary components and classes
import { SEO } from '../utils/SEO';

// Import necessary components and classes

// Define the Post component which takes a markdown file as a prop
const Post = ({ content }) => {
  const markdown = new Markdown(content);
  const seo = new SEO();

  // Parse the markdown content to HTML
  const htmlContent = markdown.parseMarkdown();

  // Generate SEO tags based on the content
  const seoTags = seo.generateMetaTags(content);

  // Return both HTML content and SEO tags to ensure they are accessible in the template
  return { htmlContent, seoTags };
};

---

<article>
  <head>
    <title>{seoTags.title}</title>
    <meta name="description" content={seoTags.description} />
    <meta name="keywords" content={seoTags.keywords.join(', ')} />
  </head>
  <section innerHTML={htmlContent} />
</article>
 -->